# ===========================================================================
# 全仓库禁止行尾转换：Git 原样保存字节，不做任何 CRLF/LF 改写。
#
# 为什么必须这样：
#   run.bat 是 GBK(ANSI) 编码 + CRLF 换行 —— 这是 cmd.exe 的硬性要求。
#   一旦被 Git 转成 LF，双击运行会报
#   「'C:\Windows\System32\cmd.exe' 不是内部或外部命令」这类莫名错误。
#   样本 PDF、xlsx 等二进制文件同样不能被改写。
# ===========================================================================
* -text
