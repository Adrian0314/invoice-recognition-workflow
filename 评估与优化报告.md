# 发票识别机器人 · 六维评估与优化报告

> 对象：`Adrian0314/invoice-recognition-workflow`（本仓库）
> 评估范围：`optimized_python/idle_invoice_bot.py`（v2 主实现，约 3100 行单文件）、
> `invoice_wf/` + `main.py`（v1 原方案）、`optimized_uipath/`（UiPath 版）
> 对标：GitHub 上 6 个成熟发票识别项目的做法（见第四节）
> 落点：**不足清单 → 可量化优化目标 → 优先级路线图 → 已落地的 v2.1 修复**

---

## 一、结论摘要

这个机器人**已经不在"能不能跑"的层次上，而是在"跑得对不对、扛不扛得住真实票据"的层次上**。
v2 相对 v1 的六项改进（模板外置、级联提取 + 质量闸门、逐字段置信度、校验反哺置信度、
交叉校验、审计轨迹）方向都对，与 GitHub 主流做法一致。

但实测发现的问题集中在两点：**① 若干"看起来实现了、实际没生效"的机制；
② 票种与输入通道的覆盖缺口**。核心结论三条：

| # | 结论 | 关键证据 |
|---|---|---|
| 1 | **最主要的不足不是算法，是"通道缺口"** —— 缺 OFD / XML 原件通道、缺二维码通道、缺铁路客票等新数电票种。这三者都是**零 OCR、可接近 100% 准确**的通道，补齐的性价比远高于继续调正则 | 铁路客票 / 航空行程单 / 通行费三种票实测被判为 `电子发票(其他)`，并误报 3 条异常，置信度掉到 **51.9%** |
| 2 | **若干已实现的机制实际是死代码 / 未接线** | ① "严重问题→字段置信度×0.6" 因中英文字段名永远匹配不上而从未生效；② `ocr_timeout` 配置项从未被读取；③ R14 内有一行空分支 |
| 3 | **解析器性能被"与发票无关的大流"拖累**，且同一份流被解压两遍 | 合成测试：16MB 图像流被解出 **256MB** 数据、耗时 **628ms** —— 而文件里真正有用的文本流只有几十字节 |

**本次已落地 11 项修复（v2.0.0 → v2.1.0）**，且**台账结论逐项不变**：
31 行 / 正常 27 / 存在提示 1 / 异常 1 / 重复 3 / 待人工 2，合计价税合计 695,976.08 元 ——
与优化前完全一致，`.github/scripts/assert_ledger.py` 断言 `[PASS]`，幂等与 `--force` 均通过。

---

## 二、评估方法与可复现证据

### 2.1 环境（零依赖极端环境，这正是该项目最有价值的定位）

```
Windows 10.0.17763 · CPython 3.13.12
PyMuPDF=False  openpyxl=False  Pillow=False  OCR=无
样本：invoices_input/ 共 24 份（11 中文增值税 + 12 国际商务 + 1 份 10 页 reportlab 样票）
```

### 2.2 基线复现（优化前 v2.0.0）

```
24 文件 → 31 行台账 | 正常 27 · 存在提示 1 · 异常 1 · 待复核 0 · 重复拦截 3 · 待人工录入 2
置信度 88.4% ~ 92.3% | 合计：金额 659,978.00 / 税额 35,818.08 / 价税合计 695,976.08
```

### 2.3 方法

不靠猜。用 4 个只读探测脚本对**每一个可疑点构造最小用例**并记录实测输出：
票面金额写法变体、新票种标题、备注干扰、配置生效性、切分逻辑、
置信度反哺是否真的打折、流解码次数与体积放大曲线。

---

## 三、六维评估

### 3.1 识别准确率

**已做得好的部分**：零依赖路径解析 22/24 份文件；故意埋的边界用例（同号重复、
勾稽不平、税号校验位错误、一页两张、无文本层扫描件）全部命中；
16 条规则中有 4 条是**交叉校验**（R13–R16），比 GitHub 上多数规则型项目更细。

**不足（全部实测）**：

| 编号 | 不足 | 实测证据 |
|---|---|---|
| A1 | **金额正则不接受整数写法**。`MONEY_STR` 强制"恰好 1–2 位小数"，遇到 `¥10600`、`¥1000`、`票价 106` 一律取不到 | 去掉样本里的 `.00` 后：**金额 / 税额 / 价税合计三项全空**，状态判"异常"，置信度 92.3% → **60.7%** |
| A2 | **大写金额完全没被利用**。"价税合计（大写）壹万元整（小写）¥10600.00" 这种自相矛盾的票面毫无反应 | 构造大小写矛盾用例：**0 条提示**，状态仍是"存在提示"，置信度 92.3% |
| A3 | **置信度区分度不足**：27 行正常票几乎全是 88.4%，差异只来自"有没有抽出 item_name / drawer"，与"读得准不准"关系很弱 | 正常行区间 88.4%–92.3%，宽度仅 3.9 个百分点；`min_confidence=0.75` 的门控从不触发（待复核 0 行） |
| A4 | **"严重问题 → 置信度打折"是死代码**。`adjust_confidence()` 用 `field in k or k in field` 匹配，而 `field` 是中文标签（"金额勾稽"）、`k` 是英文 key（`amount`）—— **永远匹配不上** | 一份勾稽不平（严重）的票，字段置信度仍是 `amount=0.9 tax=0.9 total=0.93`，一点没被压低 |
| A5 | **无字段级评测集**。24 份是"合成样本 + 结论断言"，没有逐字段 ground truth，回答不了"准确率是 96% 还是 82%" | `assert_ledger.py` 断言的是"行数区间 + 异常上限 + 置信度下限"，不是字段准确率 |
| A6 | **明细行 / 表格未抽取**（只有"主要项目"一个拼接串），多税率票无法按行勾稽 | 对标 `invoice2data` 的 `lines` / `tables` / `camelot` 插件；本项目无对应能力 |

**对标基准**：`Adiaparmar/Invoice-Extraction-VLM` 在 347 张 SROIE 上给出可比较的数字 ——
Qwen2.5-VL-7B 平均字段准确率 **95.46%**（Total 99.14%、Date 86.17%），PaddleOCR 仅 **57.13%**；
但同一份基准显示 **VLM 的表内合计匹配率只有 53.89%**。
结论明确：**关键字段要靠确定性通道（二维码 / XML / 文本层）压到 99%+，
表格与明细才是 VLM 的补充位，而不是替代位。**

### 3.2 处理性能

**现状**：24 份文件、31 行台账，CPU 侧解析总耗时约 1 秒（≈40ms/文件），单文件 2–23ms。
当前样本量下很快，但**耗时的斜率不对**。

**不足（全部实测）**：

| 编号 | 不足 | 实测证据 |
|---|---|---|
| B1 | **每个 PDF 对象被解码两遍**（第一遍找 CMap、第三遍取内容流），无缓存 | 44 个对象的样本：`_decode_stream` 被调用 **86 次** |
| B2 | **与票面无关的大流被完整解压**（扫描件的图像流），且没有体量上限 | 合成 5×2MB Flate 流：解码输出 **128MB**、耗时 **385ms**；旧实现 8 个流时 **256MB / 628ms**，斜率约 **40ms 每 MB 载荷** |
| B3 | 扫描件逐页栅格化 ×3 候选图 × 每张各起一次 OCR 子进程，无页级复用 | `_pdf_ocr()` 每页每候选各起一次子进程（每次 = 解释器冷启动 + 模型加载） |
| B4 | 无性能基线、无耗时看板。审计轨迹里有 `elapsed`，但没有断言与汇总 | 只能人工翻日志 |

**对标**：`invoice2data` 是**级联 + 按需升级**（pdfium 优先，OCR 只作最后手段）；
`Mark111113/invoice-parser` 主打 **layout 模式失败才 fallback 到 raw 模式 + 特征词打分**。
本项目的"多视图择优"已覆盖这层思路，**缺的是"不为无关负载付钱"这一课**。

### 3.3 支持的发票类型

**现状**：`detect_type()` 可识别 8 类 —— 数电普票 / 专票、电子普票 / 专票、
机动车、二手车、增值税专票 / 普票；`SUPPORTED_EXTS` 含 8 种图片 / PDF 扩展名。

**不足（全部实测）**：

| 编号 | 不足 | 实测证据 |
|---|---|---|
| C1 | **不支持 OFD** —— 而 OFD 是数电票的官方版式之一，很多开票方默认发 OFD | `.ofd in SUPPORTED_EXTS` → **False** |
| C2 | **不支持 XML** —— 数电票可下载 XML 结构化原件，**字段零误差、零 OCR** | `.xml in SUPPORTED_EXTS` → **False** |
| C3 | **不支持二维码通道**。发票二维码本身携带票据代码 / 号码 / 金额 / 日期 / 校验码，是"白送"的 ground truth | 无相关代码 |
| C4 | **三类新数电票种判错**：铁路电子客票、航空运输电子客票行程单、通行费 | 三者 `invoice_type` 全为 `电子发票(其他)`，并因票面无销售方栏而**误报 3 条异常**，置信度掉到 **51.9%** |
| C5 | `detect_type()` 的"数电"关键字判断放在最前，会被**备注误伤**：票面备注提到"数电"就抹掉专 / 普区分 | 备注写"本张为数电票红冲"后，类型从 `数电发票(普通发票)` 退化为裸的 `数电发票` |
| C6 | 老版电子发票的**校验码只被抽取、从未被校验**（校验码后 6 位是官方查验要素之一） | `check_code` 无任何规则引用 |

**对标**：`GitHub1he/QrToInvoice` 用二维码把票种判定做到规则级精度 ——
数电票按 20 位号码第 5 位细分（8 = 航空行程单、9 = 铁路客票）、
`qrType=32` 且第 4–6 位为 79 = 通行费；12 位代码后两位 11 = 电子普票 / 13 = 电子专票 /
04、05 = 折叠票 / 06、07 = 卷票 / 12 = 通行费，`qrType=33/34` = 机动车 / 二手车；
另覆盖区块链发票与财政票据。
`Mark111113/invoice-parser` 把 **PDF + XML + OFD 三种输入**都做了。

国家税务总局《增值税发票查验接口规范（标准版 V2.0）》进一步印证票种是一等公民 ——
**不同票种查验所需要素不同**：专票要"不含税金额"、普票类要"校验码后 6 位"、
机动车要"不含税价"、二手车要"车价合计"。**票种判错，后续查验必然失败。**

### 3.4 代码结构

**已做得好的部分**：单文件 3100 行但分区清晰（0 配置 / 1 模板 / 2 文本工具 / 3 提取 /
4 解析 / 5 校验 / 6 LLM / 7 台账 / 8 报告 / 9 编排 / 10 自检 / 11 CLI），
顶部有 9 条设计说明与逐项对标注释；`--selftest` / `--explain` 属于工程化加分项。

**不足**：

| 编号 | 不足 |
|---|---|
| D1 | 单文件 3100 行已接近可维护上限；`_pdf_objs` / `_decode_stream` / `pdf_text_stdlib` 等 300+ 行底层解析函数混在业务文件里 |
| D2 | **文档与代码漂移**：根 README 同时存在"13 项"与"16 条"两种规则数说法，`--explain` 输出 16 条 |
| D3 | 无 pytest 等单元测试框架，测试全靠 `--selftest` 内的过程式断言（3 个用例 + 3 个机制断言） |
| D4 | 类型注解只覆盖部分函数；`_builtin_parse` 单函数 200+ 行、职责过多 |
| D5 | v1（`invoice_wf/`）与 v2（单文件）业务规则双份维护，靠"两边同步"的口头约定，无自动一致性检查 |
| D6 | 仓库根目录散落 20 余个 `_*.py` / `_*.log` 临时脚本与 `output_backup_*` 目录 |

### 3.5 错误处理

**这是全项目最扎实的一块**：单文件失败不中断整批、绝不静默丢数据（无法识别也产出一行
"待人工录入" + 一条异常记录）、OCR 跑独立子进程隔离原生崩溃（`0xC0000005` 不会带走整批）、
台账被 Excel 占用时给中文可操作提示而不是抛栈、报错信息直接带 `pip install xxx`。

**不足（全部实测）**：

| 编号 | 不足 | 实测证据 |
|---|---|---|
| E1 | **`ocr_timeout` 配置项是摆设**：`DEFAULT_CONFIG` 写 600，代码硬编码 1200，配置从未被读取 | 源码检索：`subprocess.run(..., timeout=1200)`；`ocr_timeout` 只出现在默认配置与注释里 |
| E2 | **一票被切成两段后，碎片会变成独立台账记录** | 备注写"备注：原发票号码 26312000000000000000 已作废"后：**切出 2 段 / 解析出 2 条记录**，凭空多一行 |
| E3 | **非发票文件（合同、对账单）会产生一行"异常"记录**，污染台账与异常清单 | 输入一段普通合同文本 → 解析出 1 条记录、状态"异常"、类型"未知类型" |
| E4 | R14 内有一行空分支死代码，规则意图被静默丢弃 | `if "数电" not in itype and not no.startswith("0"): pass` |
| E5 | 加密 PDF、LZWDecode、JBIG2 等情形统一降级为"没有可用文本层"，**提示不区分原因**，不利于定位 | `_apply_one_filter` 对其余滤镜一律 `return None` |

### 3.6 可扩展性

**已做得好的部分**：模板机制（`invoice_templates.json`：`keywords` + `exclude_keywords` +
每字段多正则 + `static`，字段留空回落启发式）与规则注册表（`RULES` + `rules_disabled`）
都已落地，方向与 `invoice2data` 一致；配置三级覆盖（文件内 < `config.json` < `INVOICE_*` 环境变量）。

**不足**：

| 编号 | 不足 | 说明 |
|---|---|---|
| F1 | **模板只能加正则，不能加"解析器插件"** | `invoice2data` 有 `lines`（明细行）/ `tables` / `camelot` 三类插件；本项目只有正则一档，遇到"明细表跨列 / 跨页"的版式无解 |
| F2 | **没有"复核结果 → 模板"的回流**。人工在 `待复核.csv` 里改正了值，机器学不到任何东西 | 已列为后续项但未实现 |
| F3 | **没有模板命中率 / 字段来源统计**。审计轨迹有 `extractor`，但没有"哪个模板命中最多、哪个字段总回落到启发式"的汇总 | 版式模板库无法自增长 |
| F4 | **规则只能禁用，不能参数化**。阈值都是全局 `cfg`（如 `vendor_similarity` 对所有票种生效），无法按票种 / 供应商分设 | — |
| F5 | 新增字段要改 `FIELDS` 常量，会**同时影响 v1 台账列位**（README 已注明 v2 多了两列导致 v1 读回错位） | 双版本 schema 未做版本协商 |

---

## 四、对标项目一览（均为真实存在的参考）

| 项目 | 最值得借鉴的一点 | 本项目现状 |
|---|---|---|
| `invoice-x/invoice2data` | **模板 = keywords / exclude_keywords + 每字段多正则 + `static` + `lines` / `tables` / `camelot` 插件**；可插拔级联后端（pdfium → pdftotext → … → OCR）；输出 CSV / JSON / XML；可选 AI fallback；结果规范化到 canonical schema 后做轻校验 | 模板 ✅、级联 ✅、插件 ❌、XML 输出 ❌ |
| `GitHub1he/QrToInvoice` | **二维码即 ground truth**：图片 + PDF 双策略解析二维码，责任链识别数电票 / 区块链 / 财政票据 / 特殊票种，票种细分到号码第 5 位与 `qrType` | 完全缺失 ⛔ |
| `Mark111113/invoice-parser` | **PDF + XML + OFD 三输入**；layout 失败自动 fallback raw + 特征词打分；按购方分桶输出；双轨查验（税局 API + 浏览器截图取证） | PDF ✅（多视图择优 ≈ 其 fallback 思路）、XML / OFD ❌、查验 ❌ |
| `sanluan/einvoice` | Java 侧 PDF / OFD 双格式电子发票结构化 | OFD ❌ |
| `DreamNya/invoice-ofd2json` | **OFD 就是 ZIP**：解压取 `CustomTag.xml` + `Pages/*/Content.xml`。作者明确说明"受 OFD 格式限制，文件内部分信息缺失，无法 100% 准确" —— 提醒我们 **OFD 需与二维码 / XML 互为补充** | ❌ |
| `Adiaparmar/Invoice-Extraction-VLM` | 可复用的**字段级基准**与边缘用例矩阵：模糊 / 低对比 / 旋转 / 水印 / 过暗 / 过曝 / 压缩 **100% 成功**，噪声 60%，**部分裁切与倾斜 0%** —— 说明几何形变（透视畸变、裁切）才是真正的失败源，需要的是校正与完整性检查，而不是更强的模型 | 无评测集、无几何校正 ❌ |
| 《增值税发票查验接口规范（标准版 V2.0）》 | **票种决定查验要素**；查验 API 单次 ¥0.01–0.03、响应 200–500ms、每日调用有配额 | 未接入 |

---

## 五、不足清单与优先级

| 优先级 | 编号 | 不足 | 影响 | 成本 |
|---|---|---|---|---|
| **P0** | A4 | 严重问题不降置信度（死代码） | 门控失效，坏数据可能以高置信度入账 | 低 |
| **P0** | B1 / B2 | 重复解码 + 无关大流全解压 | 扫描件场景耗时与内存随体积放大 | 低 |
| **P0** | E1 | `ocr_timeout` 未接线 | 用户改配置无效 | 极低 |
| **P0** | E2 | 备注误切分 → 多出假行 | 台账污染 | 低 |
| **P0** | C4 | 三类新数电票种判错并误报异常 | 铁路 / 机票 / 通行费报销是高频场景 | 中 |
| **P0** | A1 | 整数金额取不到 | 整票字段丢失并判"异常" | 低 |
| **P1** | A2 | 大写金额不参与勾稽 | 漏掉最有效的一类识别错误 | 低 |
| **P1** | C1 / C2 / C3 | 缺 OFD / XML / 二维码通道 | 准确率天花板被锁在 OCR 通道水平 | 中 |
| **P1** | A5 | 无字段级评测集 | 无法度量、无法回归 | 中 |
| **P1** | E3 | 非发票文件产生"异常"行 | 收件箱噪声 | 低 |
| **P1** | A6 / F1 | 无明细行 / 表格抽取与插件 | 多税率票无法按行勾稽 | 中高 |
| **P2** | D1 / D3 | 单文件 3100 行、无 pytest | 长期维护成本 | 中 |
| **P2** | D2 | CI 配置引用指向空目录 `ci/` | 误导使用者 | 极低 |
| **P2** | C6 | 校验码只抽不验 | 老版电子票少一道校验 | 低 |
| **P2** | F2 / F3 | 无复核回流、无模板命中统计 | 模板库不能自增长 | 中 |
| **P2** | B3 | 扫描件 OCR 逐页逐候选起子进程 | 大文件慢 | 中 |
| **P2** | E5 | 加密 / LZW / JBIG2 归为同一句提示 | 定位困难 | 低 |
| **P3** | D5 / F5 | v1 / v2 schema 与规则双份维护 | 漂移风险 | 中 |

---

## 六、优化目标（可量化）

> 原则：**能用确定性通道的地方，不要交给概率模型。**
> 目标按"通道"分层设定，而不是给一个笼统的"准确率"。

### 6.1 准确率目标

| 通道 | 目标 | 验收方式 | 现状 |
|---|---|---|---|
| XML / OFD 结构化原件 | 关键字段 **100%**（解析即得，无识别环节） | 与原件字段逐项 diff | 未支持 |
| 二维码 | 票号 / 日期 / 金额 / 校验码 **≥99.5%** | 二维码解码值与票面人工核对 | 未支持 |
| PDF 文本层 | 关键字段 **≥99%**、全字段 **≥95%** | 100 张真实票 + 人工标注的字段级 P / R / F1 | 未度量（仅有结论断言） |
| OCR（图片 / 扫描件） | 关键字段 **≥95%**，几何形变样本有兜底 | 含倾斜 / 裁切 / 噪声的边缘用例矩阵 | 未度量 |
| 表格 / 明细行 | 合计行匹配 **≥85%**（显著高于 VLM 基准的 53.89%） | 明细分项与合计的自洽率 | 未支持 |

### 6.2 性能目标

| 指标 | 目标 | 现状 |
|---|---|---|
| 文本层单文件 P95 | ≤ 300ms | 实测 2–23ms（样本小），但**与体积的斜率需压平** |
| 扫描件 | ≤ 3s/页（含 OCR） | 未度量；OCR 候选图 3 份 / 页 |
| 与发票无关的流解压字节 | **0** | 旧实现 16MB 载荷解压 256MB |
| 单文件内存峰值 | ≤ 200MB | 旧实现解压后峰值可达数百 MB |
| 幂等重跑 | 全量跳过、台账零膨胀 | ✅ 已达标 |

### 6.3 票种与通道目标

- 票种：8 类 → **≥12 类**（补铁路电子客票、航空运输电子客票行程单、通行费、
  财政票据、区块链发票、机动车完备字段）
- 输入通道：PDF / 图片 → **PDF + OFD + XML + 二维码 + 图片**
- 免 OCR 直取率：收件箱文件中 **≥60%** 应能走"XML / OFD / 二维码 / 文本层"通道，零 OCR

### 6.4 质量门控目标

- 异常召回 **≥95%**
- **正常票误入"待复核" ≤ 2%** —— 当前恒定 88.4% 的置信度意味着门控形同虚设；
  需校准到"分数有区分度"（可用评测集做等距回归校准）
- 人工复核量：从"翻整张台账"降到"只看 `待复核.csv`"（机制已实现，
  但**待复核率必须真实可信**才有意义）

### 6.5 可维护性目标

- 新增一种票据版式：**0 行代码**（已达成，保持）
- 单文件 ≤ 1500 行或拆为 `invoice_wf/` 包；补 pytest（当前 3100 行 / 0 个测试文件）
- 规则条数、配置项、产出物清单由 `--explain` 单一来源生成，杜绝文档漂移

---

## 七、本次已落地的优化（v2.0.0 → v2.1.0）

**约束：保留全部原有核心功能。** 判据不是"跑得通"，而是**台账结论逐项不变**。

### 7.1 修复清单（11 项）

| # | 对应不足 | 改了什么 | 优化后实测 |
|---|---|---|---|
| 1 | A4 | `adjust_confidence()` 增加 **中文字段标签 → 英文 key 映射表**（`FIELD_ALIAS` + `_penalty_targets()`），让"严重问题 → 相关字段 ×0.6"真正生效；未知标签仍退回子串匹配以兼容自定义规则 | 勾稽不平的票：`amount/tax/total` 从 `0.9/0.9/0.93` 变为 **`0.54/0.54/0.558`**，记录置信度 90.7% → **78.7%** |
| 2 | A1 | `MONEY_STR` 的**小数部分改为可选**（`-?\d[\d,]*(?:\.\d{1,2})?`）；粘连 token 的剥离仍走严格"两位小数"规则（`_MONEY_PREFIX` 不变），避免误剥离 | 整数金额票：`amount=10000 / tax=600 / total=10600` 全部取到，状态从"异常"回到"存在提示" |
| 3 | A2 | 新增中文大写金额解析 `cn_amount_to_number()`（支持 圆/圓/圜/元、整/正、分角、一二三 小写汉字、万/亿 分段）+ **新规则 R17「大写金额与小写金额不一致」** | 真实样本形态（壹仟零陆拾圆整 / ¥1,060.00）**0 误报**；矛盾票（壹万元整 / ¥1,060.00）精确命中并给出两个数值 |
| 4 | C4 | `detect_type()` 新增三类票种识别 —— `电子发票(铁路电子客票)` / `电子发票(航空运输电子客票行程单)` / `电子发票(通行费)`；只匹配**标题级**写法，避免被明细行或备注误伤 | 三类票 `invoice_type` 全部正确；新增 `_relaxed_type()` 让 R01 / R11 / R12 对"票面无销售方栏位"的票种不再误报 |
| 5 | C4 | 新增 `_is_edigital()`：把"数电体系"判定从 `"数电" in itype` 扩展为 `数电 / 全电 / 电子发票(...)` 前缀，供 R03 / R11 / R14 共用 | 新票种不再误报"缺发票代码""数电号码位数不符" |
| 6 | C4 | `票价` 作为 `价税合计` 的兜底取值（铁路客票 / 行程单用"票价"而非"价税合计"） | 铁路客票 `total=106.0` 正常取到 |
| 7 | C4 | 新票种把「车次 + 区间」/「航班号」写入既有列『主要项目/货物名称』，不改表结构 | 台账列语义对新票种同样成立 |
| 8 | C5 | `detect_type()` 顺序调整：**专 / 普判断提到通用"数电 / 全电"之前** | 备注写"本张为数电票红冲"后，类型仍为 `数电发票(普通发票)`（修复前退化成 `数电发票`） |
| 9 | E2 | 切分加固两层：① 切分锚点从严 —— 只有**以编号标签开头且后跟票据号数字**的行才算新票起点（`_INV_START_RE`）；② 新增 `_merge_fragments()`，把不含票据要素的碎片段落并回相邻段落 | 备注含"原发票号码 …已作废"的票：从"2 段 / 2 条记录"回到 **1 段 / 1 条记录** |
| 10 | E1 | `_ocr_images(images, cfg)` 接收配置，超时改为读 `ocr_timeout`（默认 600s），两个调用点同步 | 配置项从摆设变为生效 |
| 11 | B1 / B2 | 新增 `stream_reader()` —— **流解码缓存 + 图像流跳过 + 体量上限**（`cmap_probe_max_kb` 512KB / `pdf_stream_max_mb` 16MB）；`is_image_stream()` 识别 `/Subtype /Image` 与 DCTDecode / JPXDecode / JBIG2Decode / CCITTFaxDecode | 见 7.2 |
| — | E4 | 删除 R14 内的空分支死代码（保留原检出行为不变） | 无行为变化 |

另外补了 **3 个自检用例**把这次修掉的能力固化成回归护栏：
整数金额、铁路电子客票票种识别（含"不该报销售方名称"的反向断言）、
严重问题必须压低置信度（`expect_max_conf`）。

### 7.2 性能改善（实测）

| 场景 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| 44 对象的普通样本：流解码次数 | 86 次 | **50 次** | −42% |
| 10 页样票：解码输出 / 耗时 | 0.38MB / 23ms | **0.19MB / 19ms** | −50% / −17% |
| **真实形态**（图像 XObject）32MB 载荷 | 解码全部大流、数百 MB 输出、数百 ms | **7 次解码 / 0MB 输出 / 2ms** | **随体积的开销被消除** |
| **病态形态**（不带子类型的纯 Flate 大流）16MB 载荷 | 24 次解码 / 256MB / 628ms | **12 次解码 / 128MB / 249ms** | −50% |

> 说明：真实扫描件 PDF 里 95% 以上体积都是 `/Subtype /Image` 流，
> 因此"图像流完全跳过"这一条基本消除了"文件越大越慢"的隐患。

### 7.3 回归验证（结论不变）

```
① 全量处理   31 行 | 正常 27 · 存在提示 1 · 异常 1 · 待人工录入 2 · 重复拦截 3
              合计：659,978.00 / 35,818.08 / 695,976.08     ← 与 v2.0.0 逐项一致
② assert_ledger.py                                    → [PASS]
③ 幂等重跑   已入账跳过 21 + 换名重投 2 + 同号重复 1 = 24，台账与问题清单零膨胀 → 通过 ✓
④ --force    对单个文件强制重算，台账仍 31 行（不产生重复行）              → 通过 ✓
⑤ --selftest 6 个用例 + 4 项机制断言全部通过（规则数 16 → 17，id 唯一）
⑥ 新能力抽查 大写勾稽 / 新票种 / 整数金额 全部符合预期
```

### 7.4 改动文件

| 文件 | 改动 |
|---|---|
| `optimized_python/idle_invoice_bot.py` | 版本 `2.0.0` → `2.1.0`；新增 `cn_amount_to_number` / `FIELD_ALIAS` / `_penalty_targets` / `stream_reader` / `is_image_stream` / `_is_edigital` / `_relaxed_type` / `_INV_START_RE` / `_is_invoice_start` / `_looks_like_title` / `_merge_fragments`；新增规则 R17；改 `pdf_text_stdlib` / `_pdf_text_views` / `_extract_image` / `_pdf_ocr` / `_ocr_images` / `detect_type` / `_builtin_parse` / `_split_multi_invoice` / `adjust_confidence` / R01 / R03 / R11 / R12 / R14；新增 2 个配置项与 3 个自检用例 |
| `optimized_python/README.md` | 规则数 16 → 17（新增 R17 说明）、补 2 个新配置项、新增「十一、v2.1 变更」 |
| `优化说明与方案对比.md` | 修正 CI 文件引用（`ci/invoice-ci.yml` → `.github/workflows/invoice-ci.yml`） |
| 本文件 `评估与优化报告.md` | 新增 |

---

## 八、未做项与风险（如实说明）

| 项 | 状态 | 原因 / 建议 |
|---|---|---|
| OFD / XML 输入通道 | **未做** | 属新功能而非修复，改动面大（需引入 ZIP + XML 解析）。建议作为下一阶段第一优先 —— 这是准确率上限的关键 |
| 二维码通道 | **未做** | 需要引入二维码解码库（本项目坚持零依赖）。建议做成**可选增强**：装了 `pyzbar` / `opencv` 就用，没装就回落现路径 |
| 字段级评测集（100 张真实票 + 人工标注） | **未做** | 需要真实票据，无法由我生成。这是"能否度量改进"的前提，优先级应高于任何单项优化 |
| 明细行 / 表格抽取 | **未做** | 参照 `invoice2data` 的 `lines` 插件设计，需先定"明细行"的落表结构（建议单独 sheet，避免撑爆主台账列） |
| 非发票文件不产生"异常"行（E3） | **未做** | 需新增 `非发票文件` 状态。会改变状态枚举，进而影响 `assert_ledger.py` 基准，建议单独一个变更批次做 |
| 校验码校验（C6） | **未做** | 老版电子发票校验码后 6 位需与查验平台配合才有意义，建议与查验 API 一起做 |
| LLM / VLM 兜底 | **代码已就位，默认关闭** | 建议**只对"待复核"行开启**并记录补全字段的后续人工确认率，用数据决定是否扩大；不要对它开放覆盖高置信度确定性结果的权限 |
| UiPath 版同步 | **未同步** | 本次只改 Python 版。若两版要交替跑，需把 R17 与 `_relaxed_type` 的语义同步到 `optimized_uipath/ParseInvoice.vb` |
| 全量真实票据验证 | **未做** | 环境无 PyMuPDF / openpyxl / Pillow / OCR，PyPI 不可达，OCR 与 xlsx 分支仍未实测（v2 原文档已如实记录该缺口） |

### 明确的"不该做的事"

1. **不要为了上大模型而放弃规则层。** 基准显示 VLM 的表内合计匹配率仅 53.89%，
   而本项目靠规则已经做到"多视图择优 + 交叉校验"；把关键字段交给概率模型是净损失。
2. **不要盲目引入 LayoutLM / Donut 类版面模型。** 它们的中文票据预训练极弱，
   而中文增值税 / 数电票的版式高度标准化 —— 规则 + 模板的性价比远高。
3. **不要在缺评测集的情况下继续"调正则"。** 现在每加一条正则都无法证明是净收益，
   需要先建立字段级评测集，再把每次改动变成"指标上升 / 不下降"的可验证结论。

---

## 九、复现命令

```bat
:: 零依赖自检（含本次新增的 3 个用例）
python optimized_python\idle_invoice_bot.py --selftest

:: 全量处理
python optimized_python\idle_invoice_bot.py -i invoices_input -o output

:: 台账结论断言（CI 用的同一脚本）
python .github\scripts\assert_ledger.py output\发票明细.json

:: 干跑预览（只看识别结果、不写任何文件）
python optimized_python\idle_invoice_bot.py --dry-run
```

> 本次评估用的 4 个只读探测脚本（`_opt_probe.py` / `_opt_perf.py` / `_opt_perf2.py` /
> `_opt_verify.py`）已从仓库移除，避免污染源码树 —— 仓库根目录本就散落了不少
> 同类临时脚本，建议一并清理（对应不足 D6）。


