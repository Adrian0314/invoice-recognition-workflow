# 发票识别工作流机器人 · 调研、改进与双方案对比

> 对象项目：本仓库原方案（发票自动化处理工作流 v1，见 `invoice_wf/` 与 `main.py`）
> 本文档回答三件事：**GitHub 上成熟的方案长什么样** → **本地项目据此改了什么、为什么** → **两版实现怎么选**。
>
> 产出物位置（均在仓库内）：
> - 纯 Python 单文件版：`optimized_python/idle_invoice_bot.py`（+ `invoice_templates.json`）
> - UiPath 工作流版：`optimized_uipath/`（`Main.xaml` / `Process.xaml` / `ParseInvoice.vb` / `Config.xlsx`）
> - CI：`ci/invoice-ci.yml`
> - 逐项使用说明见各目录下的 README

---

## 一、调研：GitHub 上发票识别自动化的三条路线

检索范围覆盖 GitHub 上的开源实现、UiPath 官方与社区的做法，以及带 CI 的文档流水线实践。
可归为三条路线，各自的最佳实践不同，但**有 12 条被反复验证的做法是共通的**。

### 路线 1：纯 Python 库 / 脚本（规则 + OCR）

| 代表项目 | 核心做法 |
|---|---|
| `invoice-x/invoice2data` | **模板化解析**：YAML/JSON 模板，`keywords` 选模板、`exclude_keywords` 排除、每字段可写多条正则、支持静态字段与行项目插件；**可插拔级联后端**：pdfium → pdftotext → pdfplumber → OCR（tesseract / ocrmypdf / docTR / paddleocr / gvision）；输出 CSV/JSON/XML |
| `mukeshmoni/ai-invoicr-managent` | 单一 **Workflow Orchestrator** 编排，extract / process / validate / categorize / report 各自独立模块，同类职责不混在一个文件里 |
| `automatemyjob.co.uk` 教程 | **OCR 前处理**（灰度化 + 对比度 2.0 + 锐化）、**多参数重试**（`--psm 6/4/3` 依次尝试，取前 100 字有效者）、邮件取票（IMAP）、Task Scheduler / cron 定时 |

### 路线 2：OCR + LLM 混合（近年主流）

| 代表项目 | 核心做法 |
|---|---|
| `itsritikgoyal/AI-Invoice-Processor` | **确定性解析 + LLM 兜底 + 置信度门控（confidence gating）**：先正则抽；抽不到或低置信度才调 LLM；LLM 结果只做合并；全部配置外置（`app_config.json` + 环境变量覆盖），业务代码零硬编码；带 pytest |
| `rishwanth365/Document-AI-Invoice-Data-Extraction-Pipeline` | Tesseract OCR → Gemini 结构化 → FastAPI 后端 + Streamlit 前端；强调 OCR 预处理与 JSON 校验 |
| `ShafqaatMalik/llm-based-invoice-ocr` | **双模式**：`paid`（Together AI 上的 Qwen2.5-VL-72B）与 `open_source`（Tesseract + 启发式），按成本/精度切换 |
| `Andy34G7/smart-invoice-parser` | **渐进式分层**：RegexOnly → Regex+DocTR → Text_QA → LLM，默认从最便宜的一层开始，不满意再"重试更高层" |
| `vickytilotia/InvoiceAI` | 结果落 SQLite 持久化 + Docker 化，强调可追溯 |

### 路线 3：RPA 平台（UiPath）与 CI 流水线

| 来源 | 核心做法 |
|---|---|
| UiPath 官方 *Invoice Processing with Document Understanding* | **Taxonomy 定义字段 → Digitize（OCR）→ Classify → Extract → 校验缺失必填项 → 缺则路由到 Action Center 人工校验 → 回写 Excel**；即"AI 抽取 + 人工在环" |
| UiPath 社区 *用 Codex 提示出完整发票机器人* | **REFramework + Orchestrator 队列**：每个 PDF 作为一个队列事务；正则抽取多版式；结果追加到 Excel 并记录事务状态、时间戳、错误详情；**业务异常与系统异常分开处理** |
| UiPath 教程 / `elearncourses` | REFramework 六要素：状态机四阶段、`Config.xlsx` 外置配置、系统/业务异常分离、自动重试、结构化日志、队列事务处理；命名规范（`str_`/`dt_` 前缀）、选择器稳定优先、敏感信息走 Orchestrator Assets |
| CSDN *GLM-OCR + GitHub Actions* | **仓库即收件箱**：source-repo 推文件触发 Actions，识别结果提交到 target-repo；跨仓库用 PAT 存 Secrets；即"用 Git 做归档与版本留痕" |

### 提炼：12 条可复用的最佳实践

| # | 最佳实践 | 出处 |
|---|---|---|
| 1 | 解析规则**外置成模板/配置**，新增版式不改代码 | invoice2data、AI-Invoice-Processor |
| 2 | 文本提取用**级联后端**，一档不行升下一档 | invoice2data |
| 3 | 提取质量要有**质量闸门**，不只看"有没有文字" | 多项目共同做法 |
| 4 | **逐字段置信度 + 低置信度进人工队列** | AI-Invoice-Processor、smart-invoice-parser、UiPath Validation Station |
| 5 | **确定性优先、LLM 兜底**，且 LLM 只做补充不覆盖高置信结果 | AI-Invoice-Processor |
| 6 | **渐进式分层**：从最便宜的一层开始，不满意再升级 | smart-invoice-parser |
| 7 | OCR **预处理 + 多参数重试**，取最优结果 | automatemyjob、多项目 |
| 8 | 校验要**交叉勾稽**（金额、税率、税号校验位、日期），不只查必填 | 各项目 validator |
| 9 | **去重 + 容差校验 + 供应商名相似度** | AI-Invoice-Processor |
| 10 | 全流程**结构化日志 / 审计轨迹**，可回溯"当时看到了什么" | UiPath REFramework、InvoiceAI |
| 11 | 配置与密钥**外置**，凭证走平台凭据管理 | REFramework、AI-Invoice-Processor |
| 12 | 配 **CI + 定时任务 + Secrets**，让流程可回归、可持续运行 | GitHub Actions 实践 |

---

## 二、v1 的短板诊断

对照上面的 12 条，v1（`invoice_wf/` 包）已经做得很扎实的部分：单文件失败不中断、绝不静默丢数据、
OCR 跑独立子进程防原生库崩溃、Excel 即唯一数据源、指纹+发票唯一键双去重、13 项财务级校验、
HTML 报告、长数字串文本保护。**这些都是加分项，v2 全部保留。**

真正缺的是这 6 项：

| 短板 | 具体表现 | 对应最佳实践 |
|---|---|---|
| ① 解析规则硬编码 | 数十条正则散落在 `parser.py` 里，适配一种新票据版式必须改 Python 代码 | #1 |
| ② 只有"有文本/走 OCR"两档 | 没有质量判断，也不会在文本层质量差时主动升级重试 | #2 #3 #7 |
| ③ 没有置信度概念 | 只有通过/不通过二值判断，"字段读到了但可能读错"无法表达 | #4 #5 |
| ④ 没有人工回环队列 | 异常行和正常行混在一起，人工要全表扫 | #4 |
| ⑤ 无审计轨迹 | 只能看到最终值，看不到"当时命中了哪段原文、置信度多少" | #10 |
| ⑥ 无 CI / 无测试 | 改一行代码是否改坏，只能靠手工跑一遍看异常数 | #12 |

另外发现两个**实测出来的缺陷**，v1 里也存在（这次一并修掉）：

| 缺陷 | 后果 |
|---|---|
| 多张发票共用一个文件时，第 2 张起被误判为「文件级重复」 | 一页两张的发票只会入账 1 张 —— v1 的 `pipeline.py` 里用 `committed_in_this_file` 标志规避过，说明这个坑踩过，但很容易在重构时丢失 |
| `OCR 未安装` 与 `图片需要 Pillow` 两类报错混在一起 | 用户看到"未安装 OCR 引擎"却不知道其实还差 Pillow，报错不可操作 |

---

## 三、九项改进：改了什么、为什么、效果

编号 A–I 与代码注释、README 中的编号一致。

### A. 版式模板外置（`invoice_templates.json`）

**改了什么**：新增模板机制。模板 = `keywords` + `exclude_keywords` + `type` + `fields`（每字段可多条正则）+ `static`（固定值）。
命中 keywords 最多者生效；模板字段未覆盖的部分自动回落到内置启发式解析器。

**为什么**：借鉴 invoice2data。**发票识别系统的真实维护成本，90% 花在"又来了一种新票据版式"上。**
把这件事从"改 Python / 改 XAML"降级为"加一条 JSON"，是这套系统最重要的可维护性改进。
模板里 `fields` 留空即等于纯启发式，所以模板机制**不会降低**已经跑得好的路径的准确率——它只是给已知版式加"更懂它的先验"。

**效果证据**：本次实测中 `invoice_templates.json` 生效（"已加载 2 个外部版式模板"），
数电发票走模板正则、其余走启发式，**台账结论与不加模板时逐行一致**
（31 行 / 正常 27 / 提示 1 / 异常 1 / 重复 3 / 待人工 2），证明机制是"只增不减"的。

### B. 级联文本提取 + 质量闸门

**改了什么**：
1. `PyMuPDF → 内置标准库 PDF 解析器` 两级 PDF 文本提取（不再"没装 PyMuPDF 就完全没法处理 PDF"）；
2. 新增 `text_quality()`：按**长度、可打印比例、汉字占比、数字占比、噪声符号比例、替换符/方块字**打分 0~1；
3. 分数低于 `text_quality_min`（默认 0.55）才升级去 OCR；OCR 结果与文本层结果**比质量取优**，而不是盲目覆盖。

**为什么**：v1 只看"字符数 ≥ 20"，这个判据太粗——一份文本层抽出来全是 `|||| ~~~` 的 PDF 会被当成好文本，
后续解析全是垃圾却没有任何提示。质量闸门把"文本能不能用"变成可度量的量，为升级决策提供依据（最佳实践 #2 #3）。

**效果证据**：reportlab 生成的样票，修复前后文本质量分 **0.658 → 0.932**（质量闸门正确地判定前者不可信）。

### C. 标准库 PDF 解析器（零依赖提取）

**改了什么**：实现了一个纯标准库的 PDF 文本提取器，覆盖四种实际遇到的编码形态：

| 形态 | 判定依据 | 处理 |
|---|---|---|
| 字体自带 ToUnicode CMap | `/ToUnicode N 0 R` → CMap 流含 `beginbfchar`/`beginbfrange` | 按 CMap 把 CID 映射回 Unicode |
| 预定义 UTF-16 CMap | `/Encoding /UniGB-UTF16-H`、`/UniGB-UCS2-H` 等 | 双字节码按 UTF-16BE 解 |
| 预定义 GBK CMap | `/Encoding /GBK-EUC-H` 等 | 按 GBK 解 |
| ASCII85 + Flate 滤镜链 | `/Filter [ /ASCII85Decode /FlateDecode ]` | 按顺序逐级解码（reportlab 的典型写法） |

另外解决了三个具体坑（都是本次实测踩出来的）：
- `/Font` 是**间接引用**（`/Font 1 0 R` → obj 1 才是映射表），只认内联 `<< >>` 会拿不到字体；
- 字体资源名可能含 `+`（`/F2+0`），名字字符集必须允许它；
- **字宽判错会产出"部分命中的乱码"**（非空！），所以单字节/双字节两种解都算一遍，按可读字符占比择优，而不是"非空即成功"。

**为什么**：本项目环境实测**无法安装任何第三方包**（PyPI 镜像不可达），这不是可选的优化而是必需的兜底。
顺带带来的收益是：这个版本在**任何一台只有 Python 的机器上都能跑**，交付门槛降到最低。

**效果证据**：本机（仅 Python 3.13，**无 PyMuPDF / 无 openpyxl / 无 Pillow**）处理 24 份真实样本，
**22 份成功解析、置信度 88.4%~93.1%**，其中 reportlab 的 10 张样票全部解析并正确拆成 10 行。
这一项是本次「准确性」提升里最硬的一块。

### D. 逐字段置信度 + 记录级加权 + 待复核状态

**改了什么**：
- 每个字段带 0~1 置信度，按"怎么拿到的"给基础分：模板/标签精确匹配 0.95、模板正则 0.93、启发式 0.85~0.9、**推导值 0.6**（算出来的不是读出来的）、_bad_date 0.35、缺失 0；
- 记录级置信度 = `Σ(字段置信度 × 权重) / Σ权重`，权重上调票号/日期/价税合计（各 3.0/2.0/3.0），字段缺失按 0 分计入 → **缺字段被自然惩罚**；
- 状态由 2 态扩为 5 态：`正常 / 存在提示 / 异常 / 待复核 / 待人工录入`；
- 判定顺序：**有严重问题 → 异常** > 置信度低于 `min_confidence` 或关键字段低于 `min_critical_confidence` → **待复核** > 有警告/提示 → 存在提示 > 正常。

**为什么**：这是最佳实践 #4 的直接落地。"识别对不对"这件事本质是概率问题，用二值判断会强迫所有边界情况
要么当正常放过去（漏检），要么当异常报出来（误报，人工被淹没）。**引入置信度后，人工只需要看"待复核"那几行。**
这与 UiPath 的 Validation Station / Action Center 是同一个设计思想，只是这里用 CSV 而非 UI 承载。

**效果证据**：实测中正常行置信度 88.4%~93.1%，被判"存在提示"的行（税号校验位不通过）置信度 92.6%——
分数与人工判断一致，说明打分是可信的。

### E. 校验结果反哺置信度（闭环）

**改了什么**：`adjust_confidence()` 在校验之后跑：
- 金额三值勾稽通过 → `amount/tax/total` 各 +0.05（三个字段互相印证了）；
- 统一社会信用代码校验位通过 → 对应税号 +0.07；
- 号码结构（年份前缀+省级码）对得上 → 票号 +0.03；
- 出了严重问题的字段 → 置信度 ×0.6。

**为什么**：v1 里"识别"和"校验"是两条平行线，互不影响。但这两件事本来就有强信息互补：
**校验通过说明读到的东西自洽，置信度就该更高；冲突说明至少有一个字段读错了，置信度就该降。**
把校验结论回流到置信度，等于用免费的一致性信息提高了判别力（最佳实践 #8）。

### F. 新增 4 条交叉校验（R13–R16）

v1 的 13 项规则偏"单字段合法性"。v2 补上 4 条**组合型**校验，专抓"每个字段看起来都合法、组合起来不自洽"的错误——
这类错误恰恰是单字段校验抓不到的，也是纯 OCR 误识别的最典型表现：

| 编号 | 规则 | 抓什么 |
|---|---|---|
| R13 严重 | `\|税额 − 金额×税率\|` 超过 `max(0.05, 金额×2%)` | 税率或金额/税额被读错（三者必须自洽） |
| R14 警告 | 20 位号码第 3-4 位须为合法省级区域码；前两位须等于开票年份后两位 | OCR 把票号某位数字认错 |
| R15 警告/提示 | 12 位发票代码首位须为 0；第 11-12 位票种码常见 11/13 | 发票代码误识别 |
| R16 警告 | 购销方名称相似度 ≥ 90% 且税号不同 | 名称被识别缺字，或确为关联方 |

**为什么**：财务数据的特点是有大量**内在约束**。把这些约束显式写出来，等同给识别结果做了一次"交叉验证"，
比再加 OCR 后处理更有效，成本却几乎为零。

**效果证据**：自检用例中，故意构造的"税率 6% 但税额是 13% 算出来的"被 R13 精确命中（状态=异常）；
"金额 1000+税额 60 ≠ 价税合计 1120"被 R05 命中。**这两条正是单字段校验一定漏掉的。**

### G. 规则注册表（可开关、可扩展）

**改了什么**：16 条规则统一注册进 `RULES` 列表，每条带 `id / 级别 / 标题 / 函数`；
`config.json` 里 `rules_disabled: ["R14"]` 即可单独关掉某条；规则函数抛异常会被降级为一条"警告"而不是中断流程。

**为什么**：不同企业对"什么算异常"的容忍度不同（比如有的公司允许购销同名）。
把规则变成**运行时可配置的数据**而不是代码分支，避免为了关一条规则去改代码（最佳实践 #1 的同源思想）。
新增规则也只需加一个函数、不用碰主流程。

**效果证据**：`--explain` 可列出全部 16 条规则及其启用状态（实测输出正常）；自检断言 id 唯一性通过。

### H. 审计轨迹 + 人工回环清单

**改了什么**：
1. `审计轨迹.jsonl`：每张票一行 JSON，含 `extractor / quality / confidence / confidence_map（逐字段分数）/ evidence（命中的原文片段）/ confidence_notes（加减分原因）/ issues / elapsed`；
2. `待复核.csv`：**只挑需要人看的三类行**（异常 / 待复核 / 待人工录入），列也精简到 8 列；
3. HTML 报告新增「待复核」KPI 卡片与置信度着色。

**为什么**：最佳实践 #10。出问题时最想知道的是"当时到底看到了什么"——原始文本、命中片段、分数。
只有最终值的话，只能靠重现问题来猜。至于待复核清单，是**把人工成本直接量化**的手段：
从"翻完整张台账"变成"看 3 行"。

**效果证据**：实测产出 `待复核.csv` 恰为 2 行（扫描件 + 图片，均为缺 Pillow/OCR 导致），
没有一行无关记录混入。

### I. 工程化：幂等修正 + 状态持久化 + CI + 自检

**改了什么**：
1. 修掉"多张发票文件第 2 张起误判文件级重复"的缺陷（补回 `committed_in_this_file` 标志）；
2. **修掉零依赖路径的台账覆盖缺陷（数据丢失级）**：新增内部状态文件 `台账索引.json`，
   恢复顺序改为「`发票台账.xlsx`（需 openpyxl）→ `台账索引.json` → 空表」，
   并在 README 明确写出"两条路都不可用才会覆盖旧结果"；
3. **`--force` 语义定清楚**：先把台账里对应文件的那一行摘掉再重算 —— 于是既支持
   "装好 OCR 后重算待人工录入的文件"，也支持"改完版式模板后重算受影响的票"，且不会产生重复行；
4. 报错可操作化——图片缺 Pillow 时明确说"`pip install pillow`"，而不只是抛 `No module named 'PIL'`；
5. 内置 `--selftest`（零依赖、不读文件、3 个用例 + 质量闸门 + 规则表三项断言）；`--explain` 打印生效配置；
6. `.github/workflows/invoice-ci.yml`：自检（3 OS × 3 Python 版本）→ 样本回归（含 `assert_ledger.py` 台账断言）→ 工作日定时入账 + artifact 归档；配套 `clean_output.py`（跨平台、绕开本机递归删除被拦的问题）。

**为什么**：最佳实践 #12。没有回归护栏时，改 A 项改进可能悄悄弄坏 B 项。
`assert_ledger.py` 把"正常行数下限 / 异常行数上限 / 必须存在重复拦截 / 置信度字段不得为空 / 长数字串不得被改写"
固化成断言，**改坏了 CI 会红**。

第 2 条特别值得记一笔：它是**在验证 `--force` 时才暴露出来的**——
一开始只处理 1 个文件的目录，台账直接从 31 行变成 1 行。根因是"台账读不回 → 从空表开始 → 整体覆盖"。
这类缺陷在装了 openpyxl 的机器上**不会出现**，只会在零依赖环境下静默吃掉数据，
正是"必须把降级路径也跑一遍"的典型理由。

**效果证据**：
- `--selftest` 与 `assert_ledger.py` 均实测通过；台账断言输出 `31 行 / 正常 27 / 待人工 2 / 异常 1 / 存在提示 1 → [PASS]`；
- 幂等性：同目录第二次运行 → **24 个文件全部显示 `[已处理] … 跳过`，台账仍为 31 行**；
- `--force`：对其中 1 张票强制重算 → **台账仍为 31 行**（修复前会被截成 1 行）。

---

## 四、两版实现对比

### 4.1 基本面

| 维度 | 纯 Python 单文件版 | UiPath 工作流版 |
|---|---|---|
| 交付文件 | `idle_invoice_bot.py`（单文件 + 一个模板 JSON） | 一个 UiPath 工程（`project.json` + 2 个 XAML + `Config.xlsx` + VB 源码） |
| 运行载体 | CPython 3.9+ / IDLE / 命令行 / 任务计划程序 | UiPath Studio + Robot（.NET），可发布到 Orchestrator |
| 必需依赖 | **0 个**（PDF 解析、CSV、JSON、HTML 全部内置） | UiPath 平台 + 4 个官方活动包 |
| 可选依赖 | `openpyxl`（出 xlsx 而非仅 CSV）、`pillow`（图片）、`rapidocr`（扫描件） | `UiPath.IntelligentOCR.Activities`（走 Document Understanding 时） |
| 代码规模 | ~2800 行单文件（分区清晰） | `ParseInvoice.vb` ~330 行 + 生成的 XAML |
| 台账格式 | `发票台账.xlsx`（需 openpyxl）或等价 CSV/JSON | `发票台账.xlsx` 两个 sheet |
| 配置载体 | `config.json` + `INVOICE_*` 环境变量 | `Config.xlsx`（Settings / Constants 两 sheet） |

### 4.2 能力对比

| 能力 | Python 版 | UiPath 版 |
|---|---|---|
| 首次跑通成本 | **极低**：F5 即跑，改路径只改文件头 | 中：装 Studio → 还原包 → 改 `Config.xlsx` → 运行 |
| 零依赖可运行 | **✅ 已验证** | ❌（必须装平台） |
| 无人值守 | `watch.py` 守护进程 / 任务计划程序 | **Orchestrator 触发器**，原生 |
| 多机器人并发扩容 | 自己写多进程/多机调度 | **队列 + 多机器人开箱即用** |
| 人工回环 | `待复核.csv` + HTML 报告（无 UI、无状态） | **Action Center / Validation Station**（有 UI、有状态、有审批留痕） |
| 审计合规 | JSONL 审计轨迹 + 控制台日志（自建） | **Orchestrator 执行日志 + 队列事务状态**（平台级、满足内控审计） |
| 异常语义 | `try/except` 分级，单文件失败不中断 | **系统异常重试 / 业务异常跳过**语义清晰，`RetryScope` 原生重试 |
| 复杂版式上限 | 规则 + 可选 LLM 兜底；新票据要写正则 | 可接 **Document Understanding / 生成式抽取器**，版式泛化能力更强 |
| 单张边际成本 | 0（纯本地） | 0（纯规则）；走云 OCR/DU 时按页计费 |
| 调试体验 | 断点 + print，最直接 | Studio 调试器更强但更重 |
| 版本管理 | **单文件 diff 友好** | XAML diff 噪音大（ViewState）；已用"VB 与 XAML 分离"缓解 |
| 交付给同事 | 发一个文件 | 需打包 nupkg 或共享整目录 |

### 4.3 各自的优势与代价（不粉饰）

**纯 Python 版**

- 优势：交付门槛最低（零依赖可跑）、改动即生效（改一行 → F5 → 看结果）、diff 清晰、完全离线、对机器无要求；
  逻辑集中在一个文件里，出问题定位快。
- 代价：并发扩容要自己写；人工回环没有 UI，靠 CSV 交接；复杂版式的泛化上限低于 DU；
  单文件 2800 行需要靠分区注释维持可读性。

**UiPath 版**

- 优势：生产级运行治理（Orchestrator 队列、触发器、事务状态、执行日志、Assets 凭据管理）；
  人工回环有正式的 Validation Station / Action Center；多机器人扩容是配置而非开发；
  识别天花板更高（可换 Document Understanding 抽取器）；符合大企业的变更与审计流程。
- 代价：必须有授权与 Studio 环境；开发-验证循环比 Python 长（改 VB → 重新生成 XAML → Studio 编译运行）；
  XAML 的代码评审可读性差（已通过把逻辑外置到 `.vb` 缓解）；
  **本项目环境没有 Studio，因此 UiPath 版只做到"结构可校验"，运行需你首次确认。**

### 4.4 选型建议

| 场景 | 建议 |
|---|---|
| 个人 / 小团队，月票量 < 几千张，要"今天就能跑起来" | **Python 版** |
| 财务共享中心，要求留痕可审计、要并发扩容、已有 UiPath 授权 | **UiPath 版** |
| 需要人工复核界面与审批流 | **UiPath 版**（Action Center） |
| 完全离线 / 无法安装任何第三方包 | **Python 版**（已验证零依赖可行） |
| 票据版式频繁变化、供应商多 | 两版都行；重点是用好 **A 项模板机制** |
| 最佳实践 | **两者并存**：Python 版做批量补录与离线核对（如季度抽检、历史重算），UiPath 版做日常自动入账。两版共用同一套台账列名与 Config 键名，**台账可互相读回** |

**一句话结论**：Python 版胜在"立刻可用、改起来快、零依赖"；UiPath 版胜在"运行治理、人工回环、扩容与审计"。
两者不是替代关系，业务逻辑已刻意同源（字段口径、置信度权值、R01–R16 规则编号全部对齐），可以按场景切换而不必重写规则。

---

## 五、验证记录（可复现）

### 5.1 已实测通过

环境：Windows 10.0.17763，CPython 3.13.14（**无 PyMuPDF / openpyxl / Pillow / OCR**，PyPI 不可达）。
样本：`invoice_input/` 共 24 份（11 份中文增值税样本 + 12 份国际商务样本 + 1 份 reportlab 10 页样票）。

| 检查项 | 命令 | 结果 |
|---|---|---|
| 语法 | `python idle_invoice_bot.py`（隐式编译）+ `py_compile` | 通过 |
| 内置自检 | `--selftest` | 3 用例全过；质量闸门 1.0 > 0.0；规则表 16 条 id 唯一 |
| 配置/规则清单 | `--explain` | 正常输出，16 条规则 |
| 全量处理 | `-i invoices_input -o output_v2` | **24 文件 → 31 台账行；正常 27 / 提示 1 / 异常 1 / 待复核 0 / 重复拦截 3 / 待人工 2；耗时 1.1~1.2s** |
| 置信度分布 | 同上 | 88.4% ~ 93.1% |
| 设计用例命中 | 同上 | `04` 发票级重复 ✅、`05` 勾稽不平→异常 ✅、`06` 税号校验位→存在提示 ✅、`10/11` 文件级重复提交 ✅、`07` 一页两张拆成 2 行 ✅、`08` 扫描件→待人工 ✅、`09` 图片→待人工（提示需 Pillow）✅ |
| 台账断言 | `.github/scripts/assert_ledger.py` | `[PASS]` |
| 幂等重跑 | 同目录再跑一次 | 24 个文件**全部** `[已处理] … 跳过`，台账仍 31 行、金额不变 |
| `--force` 重算 | 对 1 张票强制重算 | 台账仍 31 行（**修复前会被截成 1 行**，此缺陷即因此次验证而暴露） |
| 产出产物 | — | `发票明细.csv/json`、`台账索引.json`、`处理报告.html`、`异常记录.csv`、`待复核.csv`、`审计轨迹.jsonl` 全部生成 |
| 长数字串保护 | 检查 CSV | `发票代码`/`发票号码` 写成 `="..."`，前导 0 与 20 位完整保留 |
| 模板不改变结论 | 加 `invoice_templates.json` 前后对比 | 逐行一致 |
| Config.xlsx | `python make_config_xlsx.py` | 生成成功，6 个 OOXML 部件解析正常 |
| UiPath XAML | `python build_xaml.py` | XML 良构 ✅；InvokeCode 转义往返一致 ✅；11 个参数齐全 ✅ |
| 工程 JSON | `json.load` | `project.json` / `Taxonomy.json` / 两个模板 JSON 均合法 |

### 5.2 **未**实测（需要你确认）

| 项 | 原因 | 风险与建议 |
|---|---|---|
| Python 版的 PyMuPDF 提取路径 | 本机装不上 PyMuPDF | 低。该路径是 v1 一直在用的成熟路径；装上后会自动优先使用 |
| Python 版的 xlsx 台账输出（openpyxl 分支） | 同上 | 中。建议装 `openpyxl` 后跑一次 `-i invoices_input -o output_v2`，确认 `发票台账.xlsx` 三个区域正常；在此之前 CSV/JSON 已可用 |
| Python 版的 OCR 路径 | 无 OCR 引擎 | 中。装 `rapidocr` + `onnxruntime==1.20.1`（**勿用 1.30，老 Windows 上 import 即崩**）后跑 `08/09` 两份 |
| UiPath 工程在 Studio 中打开并运行 | 本机无 Studio、无 UiPath 包 | **中高**。XAML 结构已做程序化校验，但 Studio 的版本兼容性无法在这里验证；见 `optimized_uipath/README_UiPath.md` 第五节「10 分钟手工重建」兜底 |

### 5.3 复现命令

```bat
:: 零依赖自检
python optimized_python\idle_invoice_bot.py --selftest

:: 全量处理（本机已验证的组合）
python optimized_python\idle_invoice_bot.py -i invoices_input -o optimized_python\output_v2

:: 看识别结果但不写文件
python optimized_python\idle_invoice_bot.py --dry-run

:: 台账断言（CI 用的同一脚本）
python .github\scripts\assert_ledger.py optimized_python\output_v2\发票明细.json

:: 重新生成 UiPath 侧产物
python optimized_uipath\make_config_xlsx.py
python optimized_uipath\build_xaml.py
```

---

## 六、后续可做（本次未做，按性价比排序）

1. **接一个真实 LLM 兜底**：`config.json` 里 `llm_fallback.enabled=true` + 设 `INVOICE_LLM_API_KEY` 即可启用（代码已就位，默认关闭）。
   建议先只对"待复核"那几行开，观察补全准确率再决定是否扩大。
2. **补 OCR 前处理的多参数重试矩阵**：目前是"灰度 / 高对比锐化 / 反相"三候选，可再加"二值化阈值扫描"，
   并把每份候选的得分记进审计轨迹，便于针对特定票据调参。
3. **邮件/企业微信自动取票**：参考调研中的 IMAP 做法，把收件箱从"本地目录"扩到"邮箱附件"，`watch.py` 的调度逻辑可直接复用。
4. **给"待复核"做确认回写**：人工在 `待复核.csv` 里填正确值后，提供一个 `--apply-review` 把这些值合并回台账，
   形成"机器初筛 + 人工确认"的闭环（相当于 Python 版的 Validation Station）。
5. **UiPath 侧补 Dispatch/Performer 拆分**：把"扫描目录塞队列"与"消费队列处理"拆成两个 Process，即可多机器人并行。
6. **把 `assert_ledger.py` 的基准值提升为"按样本清单生成"**：目前基准是常数，样本变了要手改；
   可改为读 `samples_intl/样本清单.csv` 自动推算期望值。
