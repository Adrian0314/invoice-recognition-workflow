# ===========================================================================
# 发票识别工作流 · 版本库忽略规则
# ===========================================================================

# ---- Python ----
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.venv/
venv/
env/

# ---- 运行产出（流水线与 CI 会重新生成，不入库；入库会让每次运行都产生噪声 diff）----
# v1 / v2 统一写入 output/，不再有版本化输出目录
output/
output_backup_*/
output_*/
_processed/
_failed/
审计轨迹.jsonl
台账索引.json
待复核.csv

# ---- 运行时收件箱（工作目录，内容随时会变，不进版本库）----
# 规范样本在 samples/ 与 samples_intl/；invoices_input/ 是运行时的副本，
# 由 make_*.py 或 .github/scripts/build_inbox.py 生成。
invoices_input/

# ---- 本工具的会话状态（记忆文件等，不属于交付内容）----
.workbuddy/

# ---- Office 临时锁文件（Excel/WPS 打开台账时会生成）----
~$*

# ---- 本机个人配置覆盖（可能含本机绝对路径或密钥，不要提交）----
config.json

# ---- 编辑器 / 操作系统 ----
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# ---- 日志 ----
*.log

# ---- 一次性排查/临时文件（非交付内容）----
# 本仓库的交付文件都不以下划线开头；下划线开头的一律视为本地草稿。
_*
