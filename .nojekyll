# 关闭 GitHub Pages 的 Jekyll 处理，原样服务仓库里的文件。
#
# 为什么必须有它：
#   1. 网站（docs/）**直接引用** ../optimized_python/ 与 ../samples/ 下的真实文件，
#      不做副本。Jekyll 会做自己的过滤/转换，容易让这些非网页文件被改写或漏掉。
#   2. Jekyll 默认**排除**以下划线开头的文件和目录 —— 本仓库将来一旦出现这类
#      资源（如 _headers、_data），会被静默地不发布，排查起来很费时间。
#
# 因此这里彻底关掉 Jekyll，让 Pages 把仓库当纯静态目录服务。
