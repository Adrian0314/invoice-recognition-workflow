# 发票自动化处理工作流 · 使用手册

> 一句话用法：**把发票丢进 `invoices_input\` → 双击 `run.bat` 选 `1` → 打开 `output\发票台账.xlsx`。**

---

## 1. 它替你做了什么

```
你把发票放进来            程序自动完成                    你看结果
─────────────────    ─────────────────────────    ─────────────────
invoices_input\   →   识别字段 → 校验 → 去重  →   output\发票台账.xlsx
  *.pdf                一张发票一行                 output\处理报告.html
  *.png/*.jpg                                      output\发票明细.csv
                                                   output\发票明细.json
```

具体包括：

- 从 PDF / 图片里读出 **发票号码、发票代码、开票日期、购买方与销售方名称及税号、金额(不含税)、税额、价税合计、税率、项目名称、校验码、开票人**
- 做 **13 项财务级校验**（金额勾稽、号码位数、未来日期、税号校验位、购销同名…）
- 做 **两级去重**（同一张发票、同一个文件）
- 一个 PDF 里有多张发票时**自动拆成多行**，文件名后标 `[第2张/共3张]`
- 每行标注处理状态；有问题的汇总进「异常记录」页和 HTML 报告
- 反复运行**不会产生重复行**，也不会重复累加金额

---

## 2. 目录说明

| 路径 | 作用 | 你要动它吗 |
|---|---|---|
| `invoices_input\` | **发票收件箱**，把文件放这里（支持子目录，会递归扫描） | ✅ 日常用 |
| `output\` | **产出目录**：台账、报告、导出文件 | 👀 看，别删 |
| `samples\` | 演示用发票原件（11 个边界场景，中文增值税票） | 参考 |
| `samples_intl\` | 演示用国际商务发票原件（6 种版式 × 2 份，简体中文） | 参考 |
| `run.bat` | 一键菜单（双击运行） | 双击 |
| `main.py` | 主程序 | 命令行调用 |
| `watch.py` | 守护模式：落盘即自动入账 | 可选 |
| `make_samples.py` | 重新生成中文演示发票 | 可选 |
| `make_intl_samples.py` | 重新生成国际商务发票样本 | 可选 |
| `make_run_bat.py` | 重新生成 run.bat（编码坏了时用） | 备用 |
| `invoice_wf\` | 程序代码（`config.py` 是你唯一可能需要改的） | ⚙️ 需要时 |
| `README.md` | 项目说明 | 参考 |

**支持的文件类型**：`.pdf` `.png` `.jpg` `.jpeg` `.bmp` `.tif` `.tiff` `.webp`
**不支持**：`.ofd`（请先用税务系统或转换工具导出为 PDF）、`.xlsx`、压缩包

---

## 3. 三种启动方式

### 方式 A：双击 `run.bat`（推荐）

```
-- v2 单文件版（零依赖，推荐）--
1) 自检（不读文件、不装任何第三方包，秒级）
2) 处理 invoices_input 并生成报告
3) 干跑预览（只看识别结果，不写任何文件）
4) 强制重算（装好 OCR 或改完版式模板后用）

-- v1 原方案（需先 pip install -r requirements.txt）--
5) 处理 invoices_input（写入 output）
6) 干跑预览
7) 守护模式（落盘即自动入账，Ctrl+C 退出）
8) 重试之前无法识别的图片 / 扫描件
9) 生成演示发票样本

0) 退出
```

直接回车 = 选 2（v2 处理，日常最常用）。

> v2 是零依赖的单文件实现，**不装任何第三方包就能跑**；产出落在项目根的 `output\`。
> v1 需要先 `pip install -r requirements.txt`；产出同样落在 `output\`（两版统一目录，不再分开）。
> 两版台账列名与配置键名一致，可以交替使用、台账互相读回。

### 方式 B：命令行（适合批处理/自动化）

```bash
cd /d <项目根目录>
python main.py
```

### 方式 C：守护模式（无人值守）

```bash
...\python.exe watch.py --interval 5
```

跑起来后**不用再管**：往 `invoices_input\` 里丢文件，10 秒左右自动入账。按 `Ctrl+C` 退出。

---

## 4. 日常操作详解

### 场景 1：第一次跑通（用自带样本验一遍）

1. 双击 `run.bat`，选 `5` 生成演示发票（中文样本 + 国际样本 + 样票共 24 个文件）
2. 双击 `run.bat`，选 `1` 处理
3. 打开 `output\发票台账.xlsx`，对照下面的「第 5 节」看结果

预期结果（本机已启用 OCR，实测值）：

| 指标 | 数值 | 说明 |
|---|---|---|
| 台账行数 | **31** | 12 份国际样本 + 10 页样票 + 9 张中文样本 |
| 正常 | 29 | 校验全过 |
| 存在提示 | 1 | 税号校验位错误 + 购销同名（样本 06） |
| 异常 | 1 | 金额勾稽不平 180（样本 05） |
| 重复拦截 | 3 | 04 与 01 同号、10 是 01 的副本、11 是 07 的转发副本 |
| 待人工录入 | 0 | 扫描件与图片已由 OCR 识别 |
| 合计金额 / 税额 / 价税合计 | 661,088.00 / 35,884.68 / **697,152.68** | 其中 180.00 的差额来自样本 05 故意做不平 |

> 想让台账更"干净"只看中文样本：只把 `samples\` 里的文件拷进 `invoices_input\` 再跑，
> 得到 9 行（正常 7 / 提示 1 / 异常 1 / 重复拦截 3）。

### 场景 2：处理你自己的真实发票

```
① 把发票文件复制到  invoices_input\
      （可以直接整个文件夹拷进去，支持子目录）

② 双击 run.bat → 选 1

③ 打开 output\发票台账.xlsx
      先看「汇总统计」页的几个数字对不对
      再看「异常记录」页有没有要处理的
```

> **强烈建议第一次处理真实发票时先选 2（干跑）**，它在屏幕上打印每张发票的识别结果，
> 但不写任何文件。确认号码、金额、购销双方都读对了，再去正式处理。

### 场景 3：干跑预览

```bash
python main.py --dry-run
```

输出示例：

```
✓ 01_数电普票_技术服务费.pdf  [pdf-text]
   发票类型 : 数电发票(普通发票)
   发票号码 : 24312000000123456789
   开票日期 : 2026-08-15
   购买方   : 北京星辰科技有限公司  (91110108MA01ABCDEN)
   销售方   : 上海云启信息技术有限公司  (91310115MA1H8WXYQ4)
   金额/税额/合计 : 1000.0 / 60.0 / 1060.0
   税率     : 6%
   项目     : *信息技术服务*信息系统技术服务费
   校验     : 全部通过 ✓
```

`[pdf-text]` 表示读的是 PDF 自带文本层（最准）；`[ocr]` 表示走了图片识别。

### 场景 4：无人值守

```bash
python watch.py                      # 默认 5 秒轮询
python watch.py --interval 10        # 改成 10 秒
```

特性：
- 启动时先把目录里的**存量**发票处理一遍（已入账的自动跳过）
- 之后只处理**新增**的文件，且会等文件写完整（连续两轮大小不变）才动手
- 处理完一轮自动生成台账与报告

### 场景 5：按月追加（最常见）

每个月把新发票丢进 `invoices_input\`（不用清理旧文件），跑一次 `main.py`：

- 已入账的文件 → 自动跳过（日志显示 `[已处理]`）
- 新文件 → 追加到台账末尾，序号接着排
- **金额合计是整个台账的累计值**，不是本批的

---

## 5. 输出怎么读

### 5.1 `output\发票台账.xlsx`（核心）

三个工作表：

#### ① 发票明细 —— 一张发票一行

| 列 | 说明 |
|---|---|
| 序号 | 自动重排，永远连续 |
| 源文件名 | 一个 PDF 多张发票时显示为 `xxx.pdf [第2张/共3张]` |
| 发票类型 | 数电发票(普通/专用)、电子普通/专用发票、增值税专用/普通发票 |
| 发票代码 / 发票号码 | 真实票据：数电票没有发票代码（号码为 20 位）；老版是 12 位代码 + 8 位号码。本仓库的样本为了覆盖这个字段，两类都填了 12 位代码 |
| 开票日期 | 统一 `YYYY-MM-DD` |
| 购买方名称 / 税号 | 自动识别「统一社会信用代码」 |
| 销售方名称 / 税号 | 同上 |
| 金额(不含税) / 税额 / 价税合计 | 数值型，千分位格式；缺失项会用「价税合计 − 税额」自动推导 |
| 税率/征收率 | 多税率自动合并，如 `6%/13%`；支持「免税」「不征税」 |
| 主要项目/货物名称 | 取 `*类别*名称` 形式的明细行 |
| 校验码 / 开票人 | 老版电子发票的校验码、开票人姓名 |
| **处理状态** | 见第 6 节 |
| **异常/提示说明** | 该行所有问题的摘要 |
| 处理时间 | 程序处理它的时间（不是开票时间） |
| 文件指纹 | **隐藏列**，用于识别「同一文件重复提交」，不用管 |

表格自带：首行冻结 + 前两列冻结、自动筛选、金额千分位。
- **浅红底** = 异常行
- **浅黄底** = 重复/标记行

#### ② 异常记录 —— 问题清单

每行一个问题，列：`序号 / 源文件名 / 发票号码 / 问题级别 / 涉及字段 / 问题描述 / 建议处理 / 发生时间`

问题级别分 `严重` `警告` `提示` `重复` 四档，分别用不同颜色标出。

#### ③ 汇总统计

- 处理文件数、台账总行数
- 正常 / 存在提示 / 异常 / 重复拦截 / 待人工录入 / 已处理跳过 各多少
- **合计金额(不含税)、合计税额、合计价税合计**（只统计真正入账的行）
- 生成时间、台账路径
- **按销售方汇总（Top 20）**：销售方 / 张数 / 金额 / 税额 / 价税合计

### 5.2 `output\处理报告.html`

浏览器打开（或双击）。包含：

- 顶部 KPI 卡片：行数、本次新增、异常、提示、重复拦截、待人工、三个金额合计
- 异常与提示清单（按严重程度排序，带级别色块）
- 发票明细表（异常行标红、重复行标黄、待人工行标紫）
- 可直接 `Ctrl+P` 打印成 PDF 归档

### 5.3 `发票明细.csv` / `发票明细.json` / `异常记录.csv`

给其他系统用（导入财务软件、做二次分析）。CSV 用 `utf-8-sig` 编码，Excel 直接双击打开不乱码。

**发票代码 / 发票号码 / 税号在 CSV 里是按文本导出的**，形如 `="011002000001"`。
这样 Excel 打开才会显示完整的 12 位，不会把前导 0 吃掉（`011002000001` → `11002000001`）、
也不会把 20 位号码显示成 `2.4312E+19`。若你要用程序读 CSV，请用
`invoice_wf.csv_util.parse_cell()` 反解，或直接读 `发票明细.json`（那边是原始值）。

---

## 6. 五种处理状态 & 你该做什么

| 状态 | 含义 | 你要做什么 |
|---|---|---|
| **正常** | 13 项校验全过 | 不用管 |
| **存在提示** | 有警告或提示（如税号校验位不符、购销同名、红字发票） | 打开「异常记录」页看具体原因，判断是否需要联系对方重开 |
| **异常** | 有严重问题（如**金额勾稽不平**、缺少发票号码/日期、未来日期） | **必须人工核对**。最常见的是金额加不平——通常是某行明细被遮挡或票面有遮挡物 |
| **待人工录入** | 图片/扫描件无法自动识别 | 装 OCR（第 9 节）后跑 `--retry-failed`；或直接手工补录到台账 |
| **重复未入账** | 该发票已在台账中（仅 `on_duplicate=mark` 模式会出现） | 确认是否重复报销 |

**关于"重复"的两种语义（重要）**：

| 情况 | 行为 | 是否记入异常 |
|---|---|---|
| 同一个文件、同一个文件名，被反复扫描 | 静默跳过 | ❌ 不记（这是正常的幂等行为） |
| 换了文件名再提交同一份内容，或同一张发票出现在另一个文件里 | **拦截，不重复入账** | ✅ 记为「重复」 |

金额合计**只统计真正入账的行**，被拦截的重复行、待人工录入行都不会计入。

---

## 7. 命令行参数速查

### `main.py`

| 参数 | 简写 | 作用 |
|---|---|---|
| `--input 路径` | `-i` | 指定发票目录（默认 `invoices_input`） |
| `--output 路径` | `-o` | 指定台账路径（默认 `output/发票台账.xlsx`） |
| `--report 路径` | `-r` | 指定报告路径（默认 `output/处理报告.html`） |
| `--dry-run` | | 只解析打印，不写任何文件 |
| `--retry-failed` | | 重试之前「待人工录入」的图片/扫描件 |
| `--force` | | 忽略文件指纹，强制重新解析 |
| `--no-recursive` | | 只扫描顶层，不进子目录 |
| `--quiet` | | 精简输出 |
| `--wizard` | `-w` | 交互式向导 |

### `watch.py`

| 参数 | 简写 | 作用 |
|---|---|---|
| `--input` / `--output` / `--report` | `-i/-o/-r` | 同上 |
| `--interval 秒` | | 轮询间隔，默认 5 |
| `--no-recursive` | | 不进子目录 |

### 退出码

| 码 | 含义 |
|---|---|
| `0` | 全部正常 |
| `2` | 有需要人工处理的项（异常或待人工录入） |
| `3` | 台账文件被占用，写入失败 |

---

## 8. 常见调整（改 `invoice_wf\config.py`）

```python
SETTINGS = {
    "balance_tolerance": 0.02,   # 金额勾稽容差；想更严格可改 0.01
    "pdf_min_text_chars": 20,    # 低于此字符数视为扫描件，转 OCR
    "ocr_timeout": 600,          # OCR 子进程超时（秒）
    "on_duplicate": "skip",      # 重复发票：skip=不入账 / mark=入账并标记
    "reparse_same_file": False,  # 是否重复解析同一文件
}
```

**改列**：`FIELDS` 列表就是台账的列定义，改这里即可增删列、改表头、调列宽、改列顺序。
每个字段是 `{"key": ..., "header": "表头中文", "width": 列宽, "kind": "text/money/int"}`。
`kind="money"` 会自动套上千分位格式；加 `"hidden": True` 可隐藏某列。

---

## 9. 启用 OCR（识别图片和扫描件）

**电子版 PDF 发票自带文本层，识别准确率 100%，不需要 OCR。**
只有**图片**（JPG/PNG）和**扫描件 PDF** 才需要。

> ✅ **本机已经装好并验证通过**，样本里的扫描件和图片发票都成功入账了。
> 换机器或重装环境时，按下面步骤操作即可。

### 安装

必须装到**运行本程序的同一个 Python 环境**里（装到别的环境不会被用到）：

```bat
python -m pip install rapidocr "onnxruntime==1.20.1"
```

> ⚠ **关于 onnxruntime 的版本**：最新版（1.30）在本机会直接崩溃——
> 这台机器是 Windows 10.0.17763，新版 onnxruntime 的原生库加载即「访问违例」。
> **1.20.1 实测可用**。如果你换了机器，可以先试最新版，崩了再降到 1.20.1。

### 启用

```bat
C:\...\python.exe main.py --retry-failed
```

会把之前标记为「待人工录入」的文件重新识别一遍并补全台账，同时清掉它们历史上
「无文本层 / 未安装 OCR」那类已失效的记录。

### 已支持的引擎（按优先级自动选择）

**RapidOCR → PaddleOCR → Tesseract**

| 引擎 | 安装 | 说明 |
|---|---|---|
| **RapidOCR** | `pip install rapidocr onnxruntime` | **推荐**。纯 pip 安装、免编译，底层是 PP-OCR 模型，中文票据够用 |
| PaddleOCR | `pip install paddleocr paddlepaddle` | 准确率略高，但在老 Python / Windows 上极难装成（见 README 的排查记录） |
| Tesseract | 先装 Tesseract-OCR 主程序（勾选 chi_sim），再 `pip install pytesseract` | 需要额外的系统级安装 |

### 注意事项

1. 首次运行需要联网准备模型文件，可能要等一会儿，之后走本地缓存（本机已缓存好）。
2. OCR 在**独立子进程**里跑。即使 OCR 引擎崩溃，也只会让当次那张票变成「待人工录入」，
   **不会影响整批处理**，更不会丢数据。
3. 识别失败的文件**不会静默消失**：一定会有一行「待人工录入」+ 一条异常记录。
4. 台账里如果出现「待人工录入」，装好 OCR 后跑一次 `--retry-failed` 就能自动补全。
5. 扫描件/图片的识别质量取决于**清晰度和是否倾斜**。干跑一次（`--dry-run`）就能看到识别结果，
   发现关键字段读错了就人工核对那一行。

---

## 10. 常见问题（FAQ）

**Q1：双击 `run.bat` 报 `'C:\Windows\System32\cmd.exe' 不是内部或外部命令`**
文件被编辑器改成了 UTF-8 编码，破坏了批处理格式。跑一次
`python make_run_bat.py` 重新生成即可。改 `run.bat` 请用记事本「另存为 → 编码 ANSI」。

**Q2：报错说台账无法写入（PermissionError）**
`output\发票台账.xlsx` 正被 **Excel/WPS 打开**。关掉它再重跑——已入账的文件会自动跳过，不会重复。

**Q3：明明是发票，却识别不出来 / 全是待人工录入**
① 看扩展名是否在支持列表里（`.ofd` 不支持，先转 PDF）；
② 干跑一次看是 `[pdf-text]` 还是 `[ocr]`；
③ `[ocr]` 且失败 → 见第 9 节装 OCR。

**Q4：跑了两遍，为什么第二次没变化？**
这是**正常的**。文件指纹去重会把已入账的文件跳过（日志显示 `[已处理]`），
保证反复运行不会产生重复行。

**Q5：想从头重来怎么办？**
删掉整个 `output\` 目录，再跑一次即可。（台账是唯一数据源，删了就重新开始）

**Q6：`--force` 什么时候用？**
只有在**程序升级了解析逻辑、想让历史文件重新识别**时才用。它会让所有文件重新解析。
注意：如果你手工改过台账里的「发票号码」，强制重跑可能产生重复行——这种情况建议先删 `output\`。

**Q7：Excel 台账里我手工改过的内容会被覆盖吗？**
**不会**。每批处理都会先把台账读回来（包括你改过的值）再做增量追加。
但列结构（表头、列宽、颜色）会按 `config.py` 重刷，所以**不要手工插入/删除列**。

**Q8：重新生成了演示样本，为什么台账里一下子多出十几条「重复」？**
因为 PDF 每次生成时内部的隐藏文档 ID 不同 —— 内容一样但字节不一样，
同一张发票就被判成了「重复提交」。**重新生成样本后要先清空 `output\` 再重跑**：

```bat
del /q output\*
python main.py
```

真实发票不会有这个问题（原件字节是固定的），只有重新生成合成样本时才会。
另外注意：如果 `发票台账.xlsx` 正开着 Excel，`del` 会失败，先关掉再删。

**Q8：金额勾稽不平是什么意思？**
程序检查 `金额(不含税) + 税额 = 价税合计`。不平通常说明票面某个数字没识别对，
或票面本身有遮挡/裁切。去「异常记录」页看差多少，再人工核对票面。

**Q9：能同时跑两个实例吗？**
不要。两个进程同时写同一个台账会互相覆盖。要并行就各自用 `-o` 指定不同台账。

**Q10：发票很多，会不会很慢？**
电子发票（PDF 文本层）每张几十毫秒。只有走 OCR 的图片/扫描件慢（每页几秒）。

---

## 11. 每日速查卡

```
日常              双击 run.bat → 1
先验一遍          双击 run.bat → 2      （只看识别结果，不写文件）
自动收票          python watch.py      （丢进目录就自动入账）
补录扫描件        双击 run.bat → 4      （需先按第 9 节装 OCR）
看结果            output\发票台账.xlsx  （明细 / 异常记录 / 汇总统计）
                  output\处理报告.html
```

出问题先看两个地方：**控制台输出的最后几行** + **台账「异常记录」页**。
